# flask-auth-system
A secure user authentication system built with Flask. Features include user registration, login with password hashing, and SQLite database integration. Ready for deployment on Render/Railway.
